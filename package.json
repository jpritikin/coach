{
  "name": "coach",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run gen:simdata && concurrently \"npm run watch:ts\" \"hugo server -D\"",
    "watch:ts": "tsc --watch",
    "build:ts": "tsc",
    "gen:simdata": "npx tsx scripts/genSimData.ts",
    "build": "npm run gen:simdata && npm run build:ts && hugo --minify",
    "start": "hugo server",
    "test": "npx tsx test/runTests.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.7.3"
  }
}
